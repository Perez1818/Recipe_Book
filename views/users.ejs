<!DOCTYPE html>
<html>
    <head>
        <title>Recipe Book</title>
    </head>
    <body>
        <%- include("partial/navbar.ejs") %>
        <h1>Users</h1>
        <% for (let i = 0; i < users.length; i++) { %>
        <p><%= i + 1 %>. <%= users[i].name %></p>
        <% } %>
    </body>
</html>
